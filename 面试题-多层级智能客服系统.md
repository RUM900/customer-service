# 多层级智能客服系统 — 面试官版 10 题

> 出题思路：**架构设计 → 工程落地 → LLM 应用 → 生产运维**，由浅入深，考察是否真懂系统背后的权衡取舍。

---

## 第 1 题（基础）
**一句话介绍这个项目的架构，以及为什么是三层而不是一层或五层。**

### 参考答案

**架构**：三层 Agent 协作（Triage → Specialist → Supervisor），基于 LangGraph StateGraph 编排，FastAPI 提供 HTTP/SSE 接口。

**为什么不是一层**：单一 Agent 的 prompt 会极度臃肿（需要同时涵盖意图识别、领域知识、裁决规则、工具调用），且能力边界模糊，无法做权限隔离——一个 prompt injection 就能让同一 Agent 越权操作账单。

**为什么不是五层**：客服场景的复杂度用四类专业 Agent（技术/账单/产品/投诉）足以覆盖，层级越多路由成本越高、状态越难维护。三层是"够用且可控"的平衡点。

---

## 第 2 题（架构）
**用户消息进入系统后，完整走一遍链路（从收到请求到返回回复），都经历了哪些节点？**

### 参考答案

```
POST /chat/{session_id}
  → 输入安全检测（sanitize + prompt injection 检测）
  → 保存用户消息到数据库
  → graph.ainvoke:
      START → memory(会话摘要) → triage(意图+情感+紧急度)
      → 路由决策:
          ├─ faq_answer(高置信直接返回，短路)
          ├─ technical / billing / product / complaint
          │   └─ 可循环: specialist → tools → specialist
          ├─ supervisor(升级审核，可 interrupt 挂起等人工)
          └─ human_handoff(转人工)
      → END
  → 提取 final_reply → 保存 assistant 消息 → 更新会话状态
  → 返回 ChatResponse
```

**关键细节**：每轮对话用独立 thread_id，避免 checkpointer 的 `operator.add` 重复累积历史。

---

## 第 3 题（LLM 应用）
**TriageAgent 的意图识别结果如果错了，会发生什么？系统有哪些兜底？**

### 参考答案

意图错 → 路由错 → 进错 Specialist。系统有三层兜底：

1. **置信度兜底**：`intent_confidence < 0.6` 时，即使路由到 faq_answer 也会改兜底到 technical
2. **白名单映射**：`recommended_agent` 必须映射到白名单 `route_map`，未知值默认 technical
3. **异常兜底**：Triage 抛异常 → 直接返回降级回复"服务暂时繁忙"，不让错误扩散到后续 Agent

**根本缓解**：Triage 用快速小模型（qwen-turbo），同时 FAQ 高置信（score ≥ 0.75）短路不走 Agent 链路，降低误路由概率。

---

## 第 4 题（结构化输出）
**Agent 是怎么"结构化"地返回决策的（比如 SpecialistResponse）？为什么不能用纯文本让上层解析？**

### 参考答案

用 `chat_structured(messages, response_model)` 强制 LLM 输出 JSON 并反序列化为 Pydantic 模型：

```python
# base.py（抽象基类）
@abstractmethod
async def chat_structured(self, messages, response_model) -> BaseModel
```

底层实现因 Provider 而异：
- **OpenAI/DashScope**：`response_format={"type":"json_object"}` + JSON mode
- **Claude**：tool_use 机制或 JSON 解析

**为什么不用纯文本**：
- 纯文本解析不稳定，格式微变就崩
- 无法拿到强类型语义字段（`is_resolved: bool`、`needs_escalation: bool`、`tools_to_use: list[str]`）
- Pydantic 校验能拦截 LLM 幻觉产生的非法字段（如不存在的工具名）

**这也是多 Provider 抽象的核心价值**：抹平各家结构化输出差异，上层 Agent 只认 Pydantic，不关心供应商。

---

## 第 5 题（工具/安全）
**Specialist 可以调用工具，如果 LLM 被 prompt injection 诱导，反复请求某个危险工具怎么办？**

### 参考答案

五层防御，层层递进：

1. **能力白名单**：Agent 只能调用注册到 `ToolRegistry` 的工具，无法凭空调用未注册的（如 `curl`、`exec` 等）
2. **工具去重**：`executed` 集合记录已成功执行的工具，`pending` 过滤后不再重复执行
3. **轮次封顶**：`max_tool_rounds=2`，强制终止死循环
4. **HITL 兜底**：涉及资金/高风险操作必须 supervisor 审核，且可用 `interrupt()` 挂起等人工批准
5. **输入消毒**：入站先过 `sanitize_user_input()` + `detect_prompt_injection()`

**核心哲学**：LLM 负责"生成内容"，代码层负责"约束行为"——不依赖 LLM 自我约束。

---

## 第 6 题（记忆/状态）
**LangGraph 的 checkpointer 和业务数据库都存了数据，它们的分工是什么？为什么不合并？**

### 参考答案

| 维度 | Checkpointer | 业务数据库 |
|------|-------------|-----------|
| 存什么 | 一轮对话的中间态（triage_result、tool_results、interrupt 暂停点） | 终态（聊天记录、会话状态、工单、审核记录） |
| 生命周期 | 每轮销毁重建 | 永久保存 |
| 访问者 | LangGraph 图引擎 | 业务 API + 前端 |
| 核心用途 | 图内部流转 + HITL 恢复 | 历史回放 + 业务查询 |

**为什么不合并**：
- 生命周期不同：中间态是"计算过程中的临时变量"，终态是"业务结果"
- 访问模式不同：中间态由 `operator.add` 增量写入，终态是业务 CRUD
- 每轮独立 thread 的机制也依赖 checkpointer，跟业务库解耦才能独立扩展

---

## 第 7 题（并发/性能）
**如果用户量突然增长，系统哪些环节会成为瓶颈？怎么扩展？**

### 参考答案

**瓶颈排序**：LLM 调用（最贵最慢）> 向量检索 > 数据库 > 单进程 GIL

**扩展手段**：

| 瓶颈 | 手段 |
|------|------|
| LLM 成本 | 提高 FAQ 高置信命中率（短路不调 LLM）；模型分层（简单问题用小模型 qwen-turbo）；异步并发 |
| 向量检索 | ChromaDB 换 Qdrant/Milvus（抽象层已隔离，切换配置即可） |
| 数据库 | SQLite → PostgreSQL，连接池调优 |
| 应用 | 无状态多实例水平扩展（checkpointer 切 PostgresSaver 支持多进程共享） |

**核心**：客服系统瓶颈是 LLM 成本而非 QPS，优化优先级是先降 LLM 调用次数，再考虑应用层水平扩展。

---

## 第 8 题（RAG）
**FAQ 检索时，向量命中和关键词 fallback 是怎么协同的？为什么要两层？**

### 参考答案

**协同流程**：
```python
results = self._vector_search(query, category, top_k)  # 优先向量
method = "vector"
if not results:
    results = self._keyword_search(query, category, top_k)  # 降级关键词
    method = "keyword"
```
返回结果带 `method` 字段标记用的是哪种，方便观测。

**为什么两层**：
- 向量检索理解语义（"钱没到账" → 退款到账），但依赖 ChromaDB 可用、embedding API 可用
- 关键词匹配是纯本地、零依赖，作为**可用性兜底**
- 客服系统绝不能因为向量库挂了就答不上来

**核心原则**：降级比崩溃好，兜底比报错好。

---

## 第 9 题（工程/可观测）
**你怎么知道系统里每个 Agent 的表现好不好？有没有评估手段？**

### 参考答案

**当前已有的手段**：

1. **结构化日志**：每层都有 `[Triage]/[FAQ]/[Tools]` 带标识的日志，配合耗时计时（`search_time_ms`）
2. **错误追踪**：`errors` 通道累积 `AgentError(agent_name, error_type, message)`，随响应返回
3. **路由可观测**：`ChatResponse` 里带 `intent`、`sentiment`、`agent_name`，能统计路由分布和各 Agent 命中率
4. **HITL 数据反哺**：人工审核的通过/驳回率是评估 supervisor 决策质量的最好指标

**规划中**：
- Golden Dataset 离线回归（发版前跑标注集评估 F1/BLEU/人工评分）
- FAQ 命中率看板（命中率 = 不消耗 LLM 的请求比例）

---

## 第 10 题（综合/开放性）
**如果让你给这套系统加一个功能，你会加什么？为什么？**

### 参考答案

我会加 **"主动学习 + 反馈闭环"** 功能。

**思路**：把"FAQ 未命中但 LLM 成功回答"的问题，自动沉淀为新 FAQ 入库。

**实现路径**：
1. 记录未命中 query（`FAQ_CONFIDENCE_THRESHOLD` 未达标）
2. 定期聚类相似问题
3. 聚类结果推送给管理员审核确认
4. 确认后入向量库，下次同类型问题直接命中

**为什么选这个**：
- 这是"成本优化"的延续——目前 LLM 调用是最大成本
- 命中率持续上升 → LLM 调用持续下降 → 系统的**单位成本持续下降**
- 形成**越用越便宜的飞轮**，而不是越用越贵

**加分项**：说明落地路径（聚类 → 人工确认 → 入库），而不是空喊"加 AI 功能"。

---

## 💡 出题思路总结

| 考察维度 | 题号 | 考察点 |
|---------|------|--------|
| 架构理解 | 1、2 | 是否真懂三层架构和完整链路 |
| LLM 应用 | 3、4 | 意图路由容错、结构化输出 |
| 安全/工程 | 5、6 | 权限边界、状态分层 |
| 生产运维 | 7、8、9 | 扩展性、RAG 降级、可观测 |
| 综合 | 10 | 工程思维和落地能力 |

**面试官真正想看的**：不是你会背多少概念，而是遇到**"LLM 不可靠"这个现实**时，你的工程系统怎么兜底、怎么降级、怎么观测、怎么迭代。